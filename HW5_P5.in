%hw5 problem 5
%Usage mace4 -c -f HW5_P5.in > HW5_P5.out

assign(iterate_up_to, 10). % search up through order 10
formulas(assumptions).

e * x = x.    % left identity
x' * x = e.   % left inverse
(x * y) * z = x * (y * z). % associativity
A * B != B * A. % A and B do not commute: non-Abelian group

end_of_list.